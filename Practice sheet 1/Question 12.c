// Write a C program to enter any number and calculate its square root.
#include <stdio.h>
int main(){
    int value=2;
    printf("%d 's square root is %f",value,pow(value,0.5));
     
    return 0;
}
